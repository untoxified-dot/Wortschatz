// Paste the config object from your Firebase project here.
// Firebase Console → Project settings → General → "Your apps" → SDK setup and configuration.
// These values are meant to be public in client-side code — they just identify
// your project. Access is controlled by the Firestore security rules instead
// (see README.md), not by keeping this file secret.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
